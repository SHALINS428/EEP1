<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hphd.dao.CategoryMapper">

    <!-- 查询所有类目 -->
    <select id="findAll" resultType="com.hphd.model.Category">
        SELECT id, name, description, parent_id AS parentId, is_active AS isActive, created_at AS createdAt, updated_at AS updatedAt
        FROM category
    </select>

    <!-- 添加新类目 -->
    <insert id="insert" parameterType="com.hphd.model.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, description, parent_id, is_active, created_at, updated_at)
        VALUES (#{name}, #{description}, #{parentId}, #{isActive}, NOW(), NOW())
    </insert>

    <!-- 根据ID删除类目 -->
    <delete id="deleteById" parameterType="int">
        DELETE FROM category WHERE id = #{id}
    </delete>

</mapper>
